;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_
;;
;;	    ____        __      __
;;	   / __ \____  / /_  __/ /_  ____ ______
;;	  / /_/ / __ \/ / / / / __ \/ __ `/ ___/
;;	 / ____/ /_/ / / /_/ / /_/ / /_/ / /
;;	/_/    \____/_/\__, /_.___/\__,_/_/
;;	              /____/
;;
;; Created By Aditya Shakya @adi1090x
;; Edited By C. Ramprakash @CodePurble
;;
;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;; Global WM Settings

[global/wm]
; Adjust the _NET_WM_STRUT_PARTIAL top value
; Used for top aligned bars
margin-bottom = 0

; Adjust the _NET_WM_STRUT_PARTIAL bottom value
; Used for bottom aligned bars
margin-top = 0

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;; File Inclusion
; include an external file, like module file, etc.

include-file = ~/.config/polybar/colors.ini
include-file = ~/.config/polybar/modules.ini
include-file = ~/.config/polybar/user_modules.ini
include-file = ~/.config/polybar/bars.ini

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[bar/main-herbst]
monitor = ${env:MONITOR}
override-redirect = false

bottom = false

fixed-center = true

width = 100%
height = 28

offset-x = 0
offset-y = 0

; background = ${color.bg-trans}
background = ${color.nord1}

foreground = ${color.nord7}

radius-top = 1.0
radius-bottom = 1.0

line-size = 2
line-color = ${color.ac}

border-bottom-size = 0
border-color = ${color.ac}

padding = 2

module-margin = 3

font-0 = "Scientifica:size=11;2"
font-1 = "waffle:size=19;2"
font-2 = "FantasqueSansMono Nerd Font Mono-Regular:size=20;2"
font-3 = "FantasqueSansMono Nerd Font Mono-Regular:size=13;2"
font-4 = "FantasqueSansMono Nerd Font Mono-Regular:size=11;2"
font-5 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=13;2"
font-6 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=20;2"
font-7 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=16;2"
font-8 = "FantasqueSansMono Nerd Font Mono-Regular:size=16;2"

modules-left = ewmh keyboard title
modules-center = caldate
modules-right = memory cpu battery i3-volume

spacing = 0

dim-value = 1.0

tray-position = right

tray-detached = false

tray-maxsize = 16

; tray-background = ${color.bgshade4}

tray-offset-x = 0
tray-offset-y = 0

tray-padding = 0

tray-scale = 1.0

enable-ipc = true

scroll-up = herbstclient use_index +1 --skip-visible
scroll-down = herbstclient use_index -1 --skip-visible

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;; Bar Settings

[bar/secondary-herbst]
monitor = ${env:MONITOR}
override-redirect = false

bottom = false

fixed-center = true

width = 100%
height = 28

offset-x = 0
offset-y = 0

; background = ${color.bg-trans}
background = ${color.bg}

foreground = ${color.fg}

radius-top = 1.0
radius-bottom = 1.0

line-size = 2
line-color = ${color.ac}

border-bottom-size = 0
border-color = ${color.ac}

padding = 2

module-margin = 3

font-0 = "Scientifica:size=11;2"
font-1 = "waffle:size=19;2"
font-2 = "FantasqueSansMono Nerd Font Mono-Regular:size=20;2"
font-3 = "FantasqueSansMono Nerd Font Mono-Regular:size=13;2"
font-4 = "FantasqueSansMono Nerd Font Mono-Regular:size=11;2"
font-5 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=13;2"
font-6 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=20;2"
font-7 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=16;2"
font-8 = "FantasqueSansMono Nerd Font Mono-Regular:size=16;2"

modules-left = ewmh
modules-center = caldate
modules-right = keyboard title

spacing = 0

dim-value = 1.0

tray-position = none

tray-detached = false

tray-maxsize = 16

; tray-background = ${color.bgshade4}

tray-offset-x = 0
tray-offset-y = 0

tray-padding = 0

tray-scale = 1.0

enable-ipc = true

scroll-up = herbstclient use_index +1 --skip-visible
scroll-down = herbstclient use_index -1 --skip-visible

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[bar/main]
; Use either of the following command to list available outputs:
; If unspecified, the application will pick the first one it finds.
; $ polybar -m | cut -d ':' -f 1
; $ xrandr -q | grep " connected" | cut -d ' ' -f1
monitor = eDP-1-1

; Use the specified monitor as a fallback if the main one is not found.
monitor-fallback = eDP-1

; Require the monitor to be in connected state
; XRandR sometimes reports my monitor as being disconnected (when in use)
monitor-strict = false

; Tell the Window Manager not to configure the window.
; Use this to detach the bar if your WM is locking its size/position.
override-redirect = false

; Put the bar at the bottom of the screen
bottom = false

; Prefer fixed center position for the `modules-center` block
; When false, the center position will be based on the size of the other blocks.
fixed-center = true

; Dimension defined as pixel value (e.g. 35) or percentage (e.g. 50%),
; the percentage can optionally be extended with a pixel offset like so:
; 50%:-10, this will result in a width or height of 50% minus 10 pixels
width = 100%
height = 28

; Offset defined as pixel value (e.g. 35) or percentage (e.g. 50%)
; the percentage can optionally be extended with a pixel offset like so:
; 50%:-10, this will result in an offset in the x or y direction
; of 50% minus 10 pixels
offset-x = 0
offset-y = 0

; Background ARGB color (e.g. #f00, #ff992a, #ddff1023)
background = ${color.bg}

; Foreground ARGB color (e.g. #f00, #ff992a, #ddff1023)
foreground = ${color.fg}

; Background gradient (vertical steps)
;   background-[0-9]+ = #aarrggbb
;;background-0 =

; Value used for drawing rounded corners
; Note: This shouldn't be used together with border-size because the border
; doesn't get rounded
; Individual top/bottom values can be defined using:
;   radius-{top,bottom}
radius-top = 1.0
radius-bottom = 1.0

; Under-/overline pixel size and argb color
; Individual values can be defined using:
;   {overline,underline}-size
;   {overline,underline}-color
line-size = 2
line-color = ${color.ac}

border-bottom-size = 0
border-color = ${color.ac}

; Number of spaces to add at the beginning/end of the bar
; Individual side values can be defined using:
;   padding-{left,right}
padding = 2

; Number of spaces to add before/after each module
; Individual side values can be defined using:
;   module-margin-{left,right}
module-margin-left = 1
module-margin-right = 1

font-0 = "Scientifica:size=11;2"
font-1 = "waffle:size=19;2"
font-2 = "FantasqueSansMono Nerd Font Mono-Regular:size=20;2"
font-3 = "FantasqueSansMono Nerd Font Mono-Regular:size=13;2"
font-4 = "FantasqueSansMono Nerd Font Mono-Regular:size=11;2"
font-5 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=13;2"
font-6 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=20;2"
font-7 = "FantasqueSansMono Nerd Font Mono-Regular:size=20;2"

modules-left = bspwm sep keyboard sep title
modules-center = caldate
modules-right = memory sep cpu sep battery sep updates sep volume sep sysmenu

; This value is used to add extra spacing between elements
; @deprecated: This parameter will be removed in an upcoming version
spacing = 0

; Opacity value between 0.0 and 1.0 used on fade in/out
dim-value = 1.0

; Value to be used to set the WM_NAME atom
; If the value is empty or undefined, the atom value
; will be created from the following template: polybar-[BAR]_[MONITOR]
; NOTE: The placeholders are not available for custom values
wm-name = bspwm

tray-position = right

; If true, the bar will not shift its
; contents when the tray changes
tray-detached = false

; Tray icon max size
tray-maxsize = 16

tray-background = ${color.bgshade4}

; Tray offset defined as pixel value (e.g. 35) or percentage (e.g. 50%)
tray-offset-x = 0
tray-offset-y = 0

; Pad the sides of each tray icon
tray-padding = 0

; Scale factor for tray clients
tray-scale = 1.0

; Restack the bar window and put it above the
; selected window manager's root
;
; Fixes the issue where the bar is being drawn
; on top of fullscreen window's
;
; Currently supported WM's:
;   bspwm
;   i3 (requires: `override-redirect = true`)
wm-restack = bspwm

; Set a DPI values used when rendering text
; This only affects scalable fonts
; dpi =

; Enable support for inter-process messaging
; See the Messaging wiki page for more details.
enable-ipc = true

;; WM Workspace Specific
scroll-up = bspc desktop -f prev.local
scroll-down = bspc desktop -f next.local

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[bar/main_external]
; Use either of the following command to list available outputs:
; If unspecified, the application will pick the first one it finds.
; $ polybar -m | cut -d ':' -f 1
; $ xrandr -q | grep " connected" | cut -d ' ' -f1
monitor = HDMI-0

; Use the specified monitor as a fallback if the main one is not found.
monitor-fallback =

; Require the monitor to be in connected state
; XRandR sometimes reports my monitor as being disconnected (when in use)
monitor-strict = true

; Tell the Window Manager not to configure the window.
; Use this to detach the bar if your WM is locking its size/position.
override-redirect = false

; Put the bar at the bottom of the screen
bottom = false

; Prefer fixed center position for the `modules-center` block
; When false, the center position will be based on the size of the other blocks.
fixed-center = true

; Dimension defined as pixel value (e.g. 35) or percentage (e.g. 50%),
; the percentage can optionally be extended with a pixel offset like so:
; 50%:-10, this will result in a width or height of 50% minus 10 pixels
width = 100%
height = 28

; Offset defined as pixel value (e.g. 35) or percentage (e.g. 50%)
; the percentage can optionally be extended with a pixel offset like so:
; 50%:-10, this will result in an offset in the x or y direction
; of 50% minus 10 pixels
offset-x = 0
offset-y = 0

; Background ARGB color (e.g. #f00, #ff992a, #ddff1023)
background = ${color.bg}

; Foreground ARGB color (e.g. #f00, #ff992a, #ddff1023)
foreground = ${color.fg}

; Background gradient (vertical steps)
;   background-[0-9]+ = #aarrggbb
;;background-0 =

; Value used for drawing rounded corners
; Note: This shouldn't be used together with border-size because the border
; doesn't get rounded
; Individual top/bottom values can be defined using:
;   radius-{top,bottom}
radius-top = 0.0
radius-bottom = 0.0

; Under-/overline pixel size and argb color
; Individual values can be defined using:
;   {overline,underline}-size
;   {overline,underline}-color
line-size = 2
line-color = ${color.ac}

border-bottom-size = 0
border-color = ${color.ac}

; Number of spaces to add at the beginning/end of the bar
; Individual side values can be defined using:
;   padding-{left,right}
padding = 2

; Number of spaces to add before/after each module
; Individual side values can be defined using:
;   module-margin-{left,right}
module-margin-left = 1
module-margin-right = 1

font-0 = "Scientifica:size=11;2"
font-1 = "waffle:size=19;2"
font-2 = "FantasqueSansMono Nerd Font Mono-Regular:size=20;2"
font-3 = "FantasqueSansMono Nerd Font Mono-Regular:size=13;2"
font-4 = "FantasqueSansMono Nerd Font Mono-Regular:size=11;2"
font-5 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=13;2"
font-6 = "FantasqueSansMono Nerd Font Mono-Regular:weight=bold:size=20;2"
font-7 = "FantasqueSansMono Nerd Font Mono-Regular:size=20;2"

modules-left = bspwm sep keyboard sep title
modules-center = caldate
modules-right = memory sep cpu sep battery sep updates sep volume sep sysmenu

; This value is used to add extra spacing between elements
; @deprecated: This parameter will be removed in an upcoming version
spacing = 0

; Opacity value between 0.0 and 1.0 used on fade in/out
dim-value = 1.0

; Value to be used to set the WM_NAME atom
; If the value is empty or undefined, the atom value
; will be created from the following template: polybar-[BAR]_[MONITOR]
; NOTE: The placeholders are not available for custom values
wm-name = bspwm

tray-position = right

; If true, the bar will not shift its
; contents when the tray changes
tray-detached = false

; Tray icon max size
tray-maxsize = 16

tray-background = ${color.bgshade4}

; Tray offset defined as pixel value (e.g. 35) or percentage (e.g. 50%)
tray-offset-x = 0
tray-offset-y = 0

; Pad the sides of each tray icon
tray-padding = 0

; Scale factor for tray clients
tray-scale = 1.0

; Restack the bar window and put it above the
; selected window manager's root
;
; Fixes the issue where the bar is being drawn
; on top of fullscreen window's
;
; Currently supported WM's:
;   bspwm
;   i3 (requires: `override-redirect = true`)
wm-restack = bspwm

; Set a DPI values used when rendering text
; This only affects scalable fonts
; dpi =

; Enable support for inter-process messaging
; See the Messaging wiki page for more details.
enable-ipc = true

;; WM Workspace Specific
scroll-up = bspc desktop -f prev.local
scroll-down = bspc desktop -f next.local

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;; Application Settings

[settings]
; The throttle settings lets the eventloop swallow up til X events
; if they happen within Y millisecond after first event was received.
; This is done to prevent flood of update event.
;
; For example if 5 modules emit an update event at the same time, we really
; just care about the last one. But if we wait too long for events to swallow
; the bar would appear sluggish so we continue if timeout
; expires or limit is reached.
throttle-output = 5
throttle-output-for = 10

; Time in milliseconds that the input handler will wait between processing events
throttle-input-for = 30

; Reload upon receiving XCB_RANDR_SCREEN_CHANGE_NOTIFY events
screenchange-reload = true

; Compositing operators
; @see: https://www.cairographics.org/manual/cairo-cairo-t.html#cairo-operator-t
compositing-background = source
compositing-foreground = over
compositing-overline = over
compositing-underline = over
compositing-border = over
;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_
;;	    __________  ______
;;	   / ____/ __ \/ ____/
;;	  / __/ / / / / /_
;;	 / /___/ /_/ / __/
;;	/_____/\____/_/
;;
;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_
